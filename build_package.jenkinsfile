pipeline {
    agent any
    tools {
        nodejs "nodejs"
    }
    parameters {
        string(name: 'payload', defaultValue: 'payload', description: 'Who should I say hello to?')
    }
    stages {
        stage('Test Hook') {
            steps {
                echo "Will deploy test5 to ${params.payload} ${env.GIT_BRANCH}"
            }
        }
    }
    post {
        always {
            cleanWs()
        }
    }
}