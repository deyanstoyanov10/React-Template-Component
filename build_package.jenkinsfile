pipeline {
    agent any
    tools {
        nodejs "nodejs"
    }
    stages {
        stage('Test Hook') {
            steps {
                echo "Will deploy test9 t ${params.ref} ${env.GIT_BRANCH}"
            }
        }
    }
    post {
        always {
            cleanWs()
        }
    }
}