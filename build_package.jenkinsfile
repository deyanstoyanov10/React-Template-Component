pipeline {
    agent any
    tools {
        nodejs "nodejs"
    }
    stages {
        stage('Test Hook') {
            steps {
                echo "Will deploy to ${payload}"
            }
        }
    }
    post {
        always {
            cleanWs()
        }
    }
}