pipeline {
    agent any
    tools {
        nodejs "nodejs"
    }
    stages {
        stage('Test Hook') {
            steps {
                echo "Will deploy test8 t ${params.ref}"
            }
        }
    }
    post {
        always {
            cleanWs()
        }
    }
}