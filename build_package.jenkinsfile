pipeline {
    agent any
    tools {
        nodejs "nodejs"
    }
    stages {
        stage('Test Hook') {
            steps {
                echo "Will deploy test8 t ${env.GIT_BRANCH}"
            }
        }
    }
    post {
        always {
            cleanWs()
        }
    }
}